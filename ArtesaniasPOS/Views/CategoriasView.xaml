<UserControl x:Class="ArtesaniasPOS.UI.Views.Configuracion.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulario -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" BorderBrush="#ddc0b8"
                BorderThickness="1" Padding="20,16" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Nombre de la categoría" FontFamily="Segoe UI" FontSize="12"
                           FontWeight="SemiBold" Foreground="#56423c" Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Style="{StaticResource InputBoxWarm}" Margin="0,0,10,0"
                             Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding GuardarCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Grid.Column="1" Style="{StaticResource ButtonPrimary}" Padding="18,9"
                            Margin="0,0,6,0" Command="{Binding GuardarCommand}"
                            Content="{Binding TextoBoton}"/>
                    <Button Grid.Column="2" Style="{StaticResource ButtonGhost}" Padding="18,9"
                            Command="{Binding CancelarCommand}" Content="Cancelar"
                            Visibility="{Binding EsEdicion, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
                <Border Background="#fdecea" CornerRadius="8" Padding="10,8" Margin="0,10,0,0"
                        Visibility="{Binding MensajeError, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="{Binding MensajeError}" FontFamily="Segoe UI" FontSize="11.5" Foreground="#ba1a1a"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Lista -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#ddc0b8" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Categorias}" SelectedItem="{Binding Seleccionada}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#f3eeeb" BorderThickness="0" Background="Transparent"
                      RowStyle="{StaticResource GridRowWarm}"
                      ColumnHeaderStyle="{StaticResource GridHeaderWarm}"
                      CellStyle="{StaticResource GridCellWarm}"
                      FontFamily="Segoe UI" FontSize="12" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="NOMBRE" Binding="{Binding Nombre}" Width="*"/>
                    <DataGridTemplateColumn Header="ESTADO" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="9,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#fdecea"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Activo}" Value="True">
                                                    <Setter Property="Background" Value="#e6f0e9"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontFamily="Segoe UI" FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Inactiva"/>
                                                <Setter Property="Foreground" Value="#ba1a1a"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="True">
                                                        <Setter Property="Text" Value="Activa"/>
                                                        <Setter Property="Foreground" Value="#3f6b4f"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Cursor="Hand" Margin="0,0,4,0" ToolTip="Editar">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="Bd" Background="#f5f3f1" CornerRadius="7" Padding="8,4">
                                                                <TextBlock Text="✏️" FontSize="12"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="#f7e8e1"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Command="{Binding DataContext.ToggleActivoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="ToolTip" Value="Desactivar"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="Bd" Background="#fdecea" CornerRadius="7" Padding="8,4">
                                                                <TextBlock x:Name="Ico" Text="🗑️" FontSize="12"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="#fbd9d4"/>
                                                                </Trigger>
                                                                <DataTrigger Binding="{Binding Activo}" Value="False">
                                                                    <Setter TargetName="Bd" Property="Background" Value="#e6f0e9"/>
                                                                    <Setter TargetName="Ico" Property="Text" Value="✅"/>
                                                                </DataTrigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activo}" Value="False">
                                                        <Setter Property="ToolTip" Value="Reactivar"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
