<UserControl x:Class="ArtesaniasPOS.Views.Wizard.WelcomeStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                    MaxWidth="500">

            <!-- Ícono grande y amigable -->
            <Border Width="120" Height="120"
                    CornerRadius="60"
                    Margin="0,0,0,30"
                    HorizontalAlignment="Center">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#8B5CF6" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="🛍️" FontSize="50"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- Título del producto -->
            <TextBlock Text="{Binding TituloProducto}"
                       FontSize="36" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#8B5CF6" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>

            <!-- Mensaje de bienvenida -->
            <TextBlock Text="{Binding MensajeBienvenida}"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="#374151"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <!-- Descripción -->
            <TextBlock Text="{Binding Descripcion}"
                       FontSize="14"
                       Foreground="#6B7280"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- Indicadores de pasos -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,10">

                <Border Width="50" Height="6" CornerRadius="3"
                        Background="#3B82F6" Margin="4,0"/>
                <Border Width="50" Height="6" CornerRadius="3"
                        Background="#E5E7EB" Margin="4,0"/>
                <Border Width="50" Height="6" CornerRadius="3"
                        Background="#E5E7EB" Margin="4,0"/>
                <Border Width="50" Height="6" CornerRadius="3"
                        Background="#E5E7EB" Margin="4,0"/>
            </StackPanel>

            <TextBlock Text="Paso 1 de 4 — Empecemos"
                       FontSize="12"
                       Foreground="#9CA3AF"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
